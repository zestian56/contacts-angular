import { TableRowColorDirective } from './table-row-color.directive';

describe('TableRowColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TableRowColorDirective();
    expect(directive).toBeTruthy();
  });
});
